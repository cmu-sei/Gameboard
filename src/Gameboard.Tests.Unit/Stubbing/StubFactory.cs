using System.Reflection;
using Gameboard.Api.Controllers;

namespace Gameboard.Test.Stubbing;

internal static class StubFactory
{
    // public static T? Stub<T>() where T : class
    // {
    //     var constructors = typeof(T).GetConstructors(BindingFlags.CreateInstance | BindingFlags.NonPublic | BindingFlags.Public);

    //     if (constructors.Count() == 0)
    //         throw new ConstructorMatchException<T>();

    //     return default(T);
    // }

    // public static void Thing()
    // {
    //     typeof(UnityGameController).COnstructo
    // }
}
